---

name: Testing

on:
  push:
  pull_request:
    branches:
      - main

jobs:

  build-ubuntu20:
    runs-on: ubuntu-20.04
    steps:
      - name: Install Go 1.17
        uses: actions/setup-go@v1
        with:
          go-version: 1.17
      - name: Install dependencies
        run: |
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qq update
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qqy install \
          wget debootstrap systemd-container libglib2.0-dev libostree-dev

      - name: Checkout
        uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          . .envrc
          task deps:install

      - name: Build default config
        run: |
          . .envrc
          task config

      - name: Generate example signing keys
        run: |
          . .envrc
          task demo:keygen

      - name: Download example OS
        run: |
          mkdir -p cache/debos
          cd cache/debos
          wget https://github.com/system-transparency/example-os/releases/download/v0.1/ubuntu-focal-amd64.cpio.gz
          wget https://github.com/system-transparency/example-os/releases/download/v0.1/ubuntu-focal-amd64.vmlinuz

      - name: Generate and Sign OS Package
        run: |
          . .envrc
          task demo:ospkg

      - name: Upload example OS Package
        uses: actions/upload-artifact@v2
        with:
          name: ospkg
          path: out/ospkgs/*
          retention-days: 1

      - name: Set Boot Mode to network
        run: |
          sed -i '/^ST_BOOT_MODE=local/s/^/#/g' st.config
          sed -i '/^#ST_BOOT_MODE=network/s/^#//g' st.config

      - name: Build network MBR bootloader installation
        run: |
          . .envrc
          task image-mbr

      - name: Build network EFI application installation
        run: |
          . .envrc
          task image-efi

      - name: Upload network installations
        uses: actions/upload-artifact@v2
        with:
          name: stboot_network_images
          path: out/stboot_*_installation.img
          retention-days: 1

      - name: Set Boot Mode to local
        run: |
          sed -i '/^ST_BOOT_MODE=network/s/^/#/g' st.config
          sed -i '/^#ST_BOOT_MODE=local/s/^#//g' st.config

      - name: Build local MBR bootloader installation
        run: |
          . .envrc
          task image-mbr

      - name: Build local EFI application installation
        run: |
          . .envrc
          task image-efi

      - name: Upload local installations
        uses: actions/upload-artifact@v2
        with:
          name: stboot_local_images
          path: out/stboot_*_installation.img
          retention-days: 1

  run-network-mbr:
    runs-on: ubuntu-20.04
    needs:
      - build-ubuntu20
    steps:
      - name: Install QEMU
        run: |
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qq update
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qqy install qemu-system-x86

      - name: Install swtpm v0.7.0
        run: |
          wget https://github.com/system-transparency/libtpms/releases/download/v0.9.0/libtpms0_0.9.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm_0.7.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm-libs_0.7.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm-tools_0.7.0_amd64.deb
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qqy install ./*.deb

      - name: Checkout
        uses: actions/checkout@v2

      - name: Build default config
        run: |
          . .envrc
          task config

      - name: Set Boot Mode to network
        run: |
          sed -i '/^ST_BOOT_MODE=local/s/^/#/g' st.config
          sed -i '/^#ST_BOOT_MODE=network/s/^#//g' st.config

      - name: Download network installations
        uses: actions/download-artifact@v2
        with:
          name: stboot_network_images
          path: out

      - name: Download example OS Package
        uses: actions/download-artifact@v2
        with:
          name: ospkg
          path: out/ospkgs

      - name: Boot MBR bootloader installation using QEMU
        run: |
          . .envrc
          .github/workflows/scripts/test-qemu.sh mbr

  run-network-hybrid:
    runs-on: ubuntu-20.04
    needs:
      - build-ubuntu20
    steps:
      - name: Install QEMU
        run: |
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qq update
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qqy install qemu-system-x86

      - name: Install swtpm v0.7.0
        run: |
          wget https://github.com/system-transparency/libtpms/releases/download/v0.9.0/libtpms0_0.9.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm_0.7.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm-libs_0.7.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm-tools_0.7.0_amd64.deb
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qqy install ./*.deb

      - name: Checkout
        uses: actions/checkout@v2

      - name: Build default config
        run: |
          . .envrc
          task config

      - name: Set Boot Mode to network
        run: |
          sed -i '/^ST_BOOT_MODE=local/s/^/#/g' st.config
          sed -i '/^#ST_BOOT_MODE=network/s/^#//g' st.config

      - name: Download network installations
        uses: actions/download-artifact@v2
        with:
          name: stboot_network_images
          path: out

      - name: Download example OS Package
        uses: actions/download-artifact@v2
        with:
          name: ospkg
          path: out/ospkgs

      - name: Boot MBR bootloader installation using QEMU with EFI firmware
        run: |
          . .envrc
          .github/workflows/scripts/test-qemu.sh mbr efi

  run-network-efi:
    runs-on: ubuntu-20.04
    needs:
      - build-ubuntu20
    steps:
      - name: Install QEMU
        run: |
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qq update
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qqy install qemu-system-x86

      - name: Install swtpm v0.7.0
        run: |
          wget https://github.com/system-transparency/libtpms/releases/download/v0.9.0/libtpms0_0.9.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm_0.7.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm-libs_0.7.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm-tools_0.7.0_amd64.deb
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qqy install ./*.deb

      - name: Checkout
        uses: actions/checkout@v2

      - name: Build default config
        run: |
          . .envrc
          task config

      - name: Set Boot Mode to network
        run: |
          sed -i '/^ST_BOOT_MODE=local/s/^/#/g' st.config
          sed -i '/^#ST_BOOT_MODE=network/s/^#//g' st.config

      - name: Download network installations
        uses: actions/download-artifact@v2
        with:
          name: stboot_network_images
          path: out

      - name: Download example OS Package
        uses: actions/download-artifact@v2
        with:
          name: ospkg
          path: out/ospkgs

      - name: Boot EFI application installation using QEMU
        run: |
          . .envrc
          .github/workflows/scripts/test-qemu.sh efi

  run-local-mbr:
    runs-on: ubuntu-20.04
    needs:
      - build-ubuntu20
    steps:
      - name: Install QEMU
        run: |
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qq update
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qqy install qemu-system-x86

      - name: Install swtpm v0.7.0
        run: |
          wget https://github.com/system-transparency/libtpms/releases/download/v0.9.0/libtpms0_0.9.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm_0.7.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm-libs_0.7.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm-tools_0.7.0_amd64.deb
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qqy install ./*.deb

      - name: Checkout
        uses: actions/checkout@v2

      - name: Build default config
        run: |
          . .envrc
          task config

      - name: Download local installations
        uses: actions/download-artifact@v2
        with:
          name: stboot_local_images
          path: out

      - name: Boot MBR bootloader installation using QEMU
        run: |
          . .envrc
          .github/workflows/scripts/test-qemu.sh mbr

  run-local-hybrid:
    runs-on: ubuntu-20.04
    needs:
      - build-ubuntu20
    steps:
      - name: Install QEMU
        run: |
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qq update
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qqy install qemu-system-x86

      - name: Install swtpm v0.7.0
        run: |
          wget https://github.com/system-transparency/libtpms/releases/download/v0.9.0/libtpms0_0.9.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm_0.7.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm-libs_0.7.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm-tools_0.7.0_amd64.deb
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qqy install ./*.deb

      - name: Checkout
        uses: actions/checkout@v2

      - name: Build default config
        run: |
          . .envrc
          task config

      - name: Download local installations
        uses: actions/download-artifact@v2
        with:
          name: stboot_local_images
          path: out

      - name: Boot MBR bootloader installation using QEMU with EFI firmware
        run: |
          . .envrc
          .github/workflows/scripts/test-qemu.sh mbr efi

  run-local-efi:
    runs-on: ubuntu-20.04
    needs:
      - build-ubuntu20
    steps:
      - name: Install QEMU
        run: |
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qq update
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qqy install qemu-system-x86

      - name: Install swtpm v0.7.0
        run: |
          wget https://github.com/system-transparency/libtpms/releases/download/v0.9.0/libtpms0_0.9.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm_0.7.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm-libs_0.7.0_amd64.deb
          wget https://github.com/system-transparency/swtpm/releases/download/v0.7.0/swtpm-tools_0.7.0_amd64.deb
          DEBIAN_FRONTEND=noninteractive sudo apt-get -qqy install ./*.deb

      - name: Checkout
        uses: actions/checkout@v2

      - name: Build default config
        run: |
          . .envrc
          task config

      - name: Download local installations
        uses: actions/download-artifact@v2
        with:
          name: stboot_local_images
          path: out

      - name: Boot EFI application installation using QEMU
        run: |
          . .envrc
          .github/workflows/scripts/test-qemu.sh efi


  build-ubuntu18:
    runs-on: ubuntu-18.04
    steps:
      - name: Install Go 1.17
        uses: actions/setup-go@v1
        with:
          go-version: 1.17

      - name: Checkout
        uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          . .envrc
          task deps:install

      - name: Build default config
        run: |
          . .envrc
          task config

      - name: Generate example signing keys
        run: |
          . .envrc
          task demo:keygen

      - name: Set Boot Mode to network
        run: |
          sed -i '/^ST_BOOT_MODE=local/s/^/#/g' st.config
          sed -i '/^#ST_BOOT_MODE=network/s/^#//g' st.config

      - name: Build MBR bootloader installation without ospkg
        run: |
          . .envrc
          task image-mbr

      - name: Build EFI application installation without ospkg
        run: |
          . .envrc
          task image-efi
